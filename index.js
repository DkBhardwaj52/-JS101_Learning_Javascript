// JAVASCRIPT 

const { stringify } = require("querystring");
const { rootCertificates } = require("tls");
const { stripVTControlCharacters } = require("util");

// JAVASCRIPT IS NOT IGNORE ANY ESPALLING MISTAKE BEACUSE IT IOS CASE CASE SENSITIVE SO NEVER WRITE ANYTHING WRONG

// VARIABLES : Storage location where you store data in it and  use the data at later point of time or assigned by a name.(like we cinsidered in maths x,y,num,char or by any name)

// var,let,or const(something which we want to cosiderd as constant)

// SYNTAX : The structure of programming

// NUMBER : Any numerical number s called Number like (2,45,90,...n)

// STRING : Anything which written in"...." it if it is number or any alphabet considered as string like ("29"or"aman")
// but never we write any variable in string form,NEVER START any variable with number,nor with symbol like @,#,$,,do not gap in considered variable for gap"_"





// :::NOW STARTS:::


// ALWAYS WRITE ALPHABETICAL VARIABLE VALUES IN STRING OK OTHERWISE RESULT COMES ERROR BEACUSE ANYTHING IN STRING THAT ONLY ATTACH NOT ADD;

let a = 7;

let b = 7;

console.log(a / b);

let A = "x";

let B = "y";

console.log(A / B);

// MODULO OPERATOR(%) : TO VERIFY ANY NUMBER IS DIVIBLE,MULTIPLE,EVEN,OR ODD
// FOR MUL,DIV = X%n==0; FOR EVEN = X%2==0; FOR ODD =( X%2==1 OR X%2!==0;)
// answer comes in boolean format true or false

let x = 20
let y = 15
console.log(x % 2 == 0);

console.log(y % 2 !== 0);

// EXPONENTIATION OPERATOR (**) : for square,cube,quad amnd also for square root,cube root quadroot or else you wants

console.log(4**2);
console.log(5**3);
console.log(2**4);

// for rootCertificates

console.log(16**(1/2))
console.log(216**(1/3))
console.log(256**(1/4))

// when number adding with stringify

let i = 30;

let w = "20";

console.log(i+w)







